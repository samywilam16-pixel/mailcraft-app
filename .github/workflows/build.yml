name: Build Flet APK
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - os: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        
    - name: Install Flet
      run: |
        pip install flet requests
        
    - name: Set up Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.x'
        channel: 'stable'
        
    - name: Build APK
      run: |
        flet build apk --no-public-signature
        
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: MailCraft-AI-APK
        path: build/apk/*.apk
